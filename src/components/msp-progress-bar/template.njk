<div class="msp-progress-bar {{- ' ' + params.classes if params.classes}}">
  <ol class="msp-progress-bar__list">
    {% for item in params.items %}
    <li class="msp-progress-bar__list-item" {{ 'aria-current="step"' | safe if item.active }}>
      <span class="msp-progress-bar__icon{{ ' msp-progress-bar__icon--complete' if item.complete }}"></span>
      <span class="msp-progress-bar__label">{{ item.label.html | safe if item.label.html else item.label.text }}</span>    
    </li>
    {% endfor %}
  </ol>
</div>